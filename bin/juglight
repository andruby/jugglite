#!/usr/bin/env ruby

host = '127.0.0.1'
port = ENV['PORT'] || 3000

# require 'juglight'
require_relative '../lib/juglight'

new_size = EM.set_descriptor_table_size( 20480 )
STDERR.puts "New descriptor-Table size is #{new_size}"
EM.epoll

Thin::Server.start(host, port, Juglight::App.new)
